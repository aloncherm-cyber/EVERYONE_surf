<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVERYONE_surf"
        tools:targetApi="31">
        <activity
            android:name=".Activity_login_instructor"
            android:exported="false" />
        <!-- Splash (מסך פתיחה) -->
        <activity
            android:name=".splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- מסך ראשי -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- אחרי התחברות -->
        <activity
            android:name=".MainActivity2"
            android:exported="false" /> <!-- התחברות -->
        <activity
            android:name=".Login"
            android:exported="false" /> <!-- הרשמה רגילה -->
        <activity
            android:name=".Register"
            android:exported="false" /> <!-- ✨ בחירת סוג הרשמה -->
        <activity
            android:name=".RegisterTypeActivity"
            android:exported="false" /> <!-- הרשמת מדריך -->
        <activity
            android:name=".Register_instructor"
            android:exported="false" /> <!-- מסך מדריך -->
        <activity
            android:name=".Instructor_Activity"
            android:exported="false" /> <!-- מסך משתמש -->
        <activity
            android:name=".User_Activity"
            android:exported="false" /> <!-- הוספת שיעור -->
        <activity
            android:name=".activity_add_lesson"
            android:exported="false" /> <!-- רשימת שיעורים -->
        <activity
            android:name=".LessonList"
            android:exported="false" /> <!-- פרטי שיעור -->
        <activity
            android:name=".LessonProfileActivity"
            android:exported="false" /> <!-- רשימת משתמשים -->
        <activity
            android:name=".UsersListActivity"
            android:exported="false" /> <!-- בחירת אזור -->
        <activity
            android:name=".activity_select_region"
            android:exported="false" />
    </application>

</manifest>